{
  "name": "migration-engine",
  "version": "1.0.0",
  "description": "A Migration Engine that classifies files into a folder hierarchy using an LLM",
  "main": "dist/classifier.js",
  "scripts": {
    "test": "ts-node src/test.ts",
    "build": "tsc",
    "start": "ts-node src/test.ts",
    "classify": "ts-node src/classify-file.ts",
    "batch": "ts-node src/batch-classify.ts",
    "scan": "ts-node src/scan-hierarchy.ts",
    "prune": "ts-node src/prune-hierarchy.ts"
  },
  "keywords": [
    "migration",
    "file-classification",
    "llm",
    "gemini"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.4.5",
    "mammoth": "^1.11.0",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
